syntax = "proto3";

package ai.data_store;

enum DataType {
  DATA_TYPE_INVALID = 0;
  HUGGING_FACE_DATASET = 1;
  CSV = 2;
  JSONL = 3;
  PARQUET = 4;
}

enum DataStoreMode {
  DATA_STORE_MODE_INVALID = 0;
  LOCAL_FILE = 1;
  CLOUD_STORAGE = 2;
}

message DataStore {
  string store_path = 1;
  DataStoreMode data_store_mode = 2;
  int32 auto_save_interval = 3;
  DataType data_store_type = 4;

  // TODO: Add topics and data types to store.
}
