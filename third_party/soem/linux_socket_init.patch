diff --git a/oshw/linux/nicdrv.c b/oshw/linux/nicdrv.c
index 61d7b76..e2409e3 100644
--- a/oshw/linux/nicdrv.c
+++ b/oshw/linux/nicdrv.c
@@ -152,7 +152,8 @@ int ecx_setupnic(ecx_portt *port, const char *ifname, int secondary)
    i = 1;
    r |= setsockopt(*psock, SOL_SOCKET, SO_DONTROUTE, &i, sizeof(i));
    /* connect socket to NIC by name */
-   strcpy(ifr.ifr_name, ifname);
+   strncpy(ifr.ifr_name, ifname, sizeof(ifr.ifr_name) - 1);
+   ifr.ifr_name[sizeof(ifr.ifr_name) - 1] = '\0';
    r |= ioctl(*psock, SIOCGIFINDEX, &ifr);
    ifindex = ifr.ifr_ifindex;
    strcpy(ifr.ifr_name, ifname);
@@ -164,4 +165,5 @@ int ecx_setupnic(ecx_portt *port, const char *ifname, int secondary)
    ifr.ifr_flags = ifr.ifr_flags | IFF_PROMISC | IFF_BROADCAST;
    r |= ioctl(*psock, SIOCSIFFLAGS, &ifr);
    /* bind socket to protocol, in this case RAW EtherCAT */
+   memset((void*)&sll, 0, sizeof(sll));
    sll.sll_family = AF_PACKET;
