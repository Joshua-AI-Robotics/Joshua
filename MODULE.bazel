module(
    name = "project_joshua",
    version = "0.1.0",
)

bazel_dep(name = "rules_cc", version = "0.1.1")
bazel_dep(name = "rules_python", version = "0.40.0")
bazel_dep(name = "rules_proto", version = "7.0.2")
bazel_dep(name = "rules_pkg", version = "0.10.1")
bazel_dep(name = "protobuf", version = "29.0")
bazel_dep(name = "abseil-cpp", version = "20240722.0")
bazel_dep(name = "pybind11_bazel", version = "2.13.6")
bazel_dep(name = "boost.asio", version = "1.83.0.bcr.1")
bazel_dep(name = "gflags", version = "2.2.2")
bazel_dep(name = "glog", version = "0.7.1")
bazel_dep(name = "platforms", version = "0.0.8")

# ROS2 is configured via system_ros2_rules.bzl using system-installed libraries
# See system_ros2_rules.bzl for library definitions

python = use_extension("@rules_python//python/extensions:python.bzl", "python")

# Python 3.10 - Default (for ROS2 compatibility)
# System Python has rclpy, numpy, and all ROS2 dependencies pre-installed
python.toolchain(
    is_default = True,
    python_version = "3.10",
    ignore_root_user_error = True,
)

# Python 3.11 - For non-ROS2 tools and scripts
python.toolchain(
    python_version = "3.11",
    ignore_root_user_error = True,
)

# Python 3.12 - For non-ROS2 tools and scripts (optional)
python.toolchain(
    python_version = "3.12",
    ignore_root_user_error = True,
)

pip = use_extension("@rules_python//python/extensions:pip.bzl", "pip")

# Python 3.10 dependencies (includes ROS2-related packages)
pip.parse(
    hub_name = "project_joshua_pip_deps",
    python_version = "3.10",
    requirements_lock = "//:requirements.lock",
)

# Python 3.11 dependencies (for non-ROS2 tools)
# You can create requirements_3.11.txt if needed
# pip.parse(
#     hub_name = "project_joshua_pip_deps_311",
#     python_version = "3.11",
#     requirements_lock = "//:requirements_3.11.lock",
# )

# Python 3.12 dependencies (for non-ROS2 tools)
# pip.parse(
#     hub_name = "project_joshua_pip_deps_312",
#     python_version = "3.12",
#     requirements_lock = "//:requirements_3.12.lock",
# )

use_repo(pip, "project_joshua_pip_deps")
# use_repo(pip, "project_joshua_pip_deps_311")  # Uncomment if using 3.11
# use_repo(pip, "project_joshua_pip_deps_312")  # Uncomment if using 3.12

# Custom rule for system dependencies
system_libs = use_repo_rule("//:system_deps.bzl", "system_libs_repo")
ros2_libs = use_repo_rule("//:ros2_deps_from_system.bzl", "ros2_libs_repo")

system_libs(name = "local_deps")
ros2_libs(name = "ros2_deps")
