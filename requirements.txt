# Platform base dependencies (shared: ROS nodes, simulation, training).
# Installed into every model venv before that model's optional requirements.lock.
# Model-only deps (torch, lerobot, …) live in ai/models/<model>/requirements.in.
#
# Note: rclpy is NOT on PyPI - it comes from system ROS2 installation.
# It will be available via PYTHONPATH in .bazelrc

protobuf==5.29.5
numpy>=2.0.0
glog
mujoco>=3.1.0
datasets>=2.14.0
opencv-python==4.11.0.86
Pillow==11.3.0
PyYAML
python-gflags
setuptools
tzdata>=2024.1
